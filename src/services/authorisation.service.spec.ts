import * as assert from "assert";
import { should } from "chai";
import { expect } from "chai";
should();

describe("AuthenticationService", () => {
	it("IsAuthenticated: returns true if any string is provided");
	it("IsAuthenticated: returns false if nothing is provided");
});
